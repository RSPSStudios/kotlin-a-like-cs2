// 2375 proc
fun castlewars_waitingroom_update(component0: Component) {

	if (VARP[380] <= 0) {
		if_settext("Waiting for players to join the other team.", component0)
	} else {
		if_settext("Time until next game starts: ${tostring(VARP[380])}", component0)
	}
	return 
}

// 1301 clientscript
fun soulbearer_repair_update(component0: Component, comsubid1: Int, comsubid2: Int, clock3: Int) {

	if (cc_find(component0, comsubid1) = ^true & .cc_find(component0, comsubid2) = ^true) {
		~soulbearer_repair_update(component0, calc(clientclock - clock3))
	}
	return 
}

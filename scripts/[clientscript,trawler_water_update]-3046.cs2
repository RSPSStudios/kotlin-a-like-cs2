// 3046 clientscript
fun trawler_water_update(component0: Component, comsubid1: Int, comsubid2: Int) {

	if (cc_find(component0, comsubid1) = ^true & .cc_find(component0, comsubid2) = ^true) {
		~trawler_water_update(component0)
	}
	return 
}
